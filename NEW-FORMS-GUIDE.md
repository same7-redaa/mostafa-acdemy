# 📋 نماذج التسجيل الجديدة

## ✅ النماذج المتوفرة

تم إضافة 3 نماذج تسجيل جديدة إلى النظام:

### 1️⃣ نموذج التقدم للحصول على الإجازة (IjazahForm)
**الملف:** `components/IjazahForm.tsx`
**اللون:** بنفسجي (Purple)
**الاستخدام:** للطلاب الراغبين في الحصول على إجازة قراءة القرآن

**الحقول:**
- الاسم الكامل *
- المدينة *
- العمر *
- رقم الهاتف (مع اختيار الدولة) *
- رقم الواتساب (مع اختيار الدولة) *
- المؤهل الدراسي *
- مستوى حفظ القرآن *
- مستوى القراءة والتجويد *
- ملاحظات إضافية (اختياري)

**قاعدة البيانات:** `ijazah-applications`

---

### 2️⃣ نموذج الجلسات النفسية (SessionForm - نوع: جلسات نفسية)
**الملف:** `components/SessionForm.tsx`
**اللون:** أزرق (Blue)
**الاستخدام:** للحجز للجلسات النفسية المتخصصة

**الحقول:**
- الاسم الكامل *
- المدينة *
- العمر *
- رقم الهاتف (مع اختيار الدولة) *
- رقم الواتساب (مع اختيار الدولة) *
- نوع الجلسة (أونلاين / حضوري) *
- الوقت المفضل *
- وصف موجز للحالة *

**قاعدة البيانات:** `psychological-sessions`

---

### 3️⃣ نموذج الاستشارات النفسية (SessionForm - نوع: استشارات نفسية)
**الملف:** `components/SessionForm.tsx`
**اللون:** أخضر مزرق (Teal)
**الاستخدام:** للاستشارات النفسية السريعة

**الحقول:** (نفس حقول الجلسات النفسية)

**قاعدة البيانات:** `psychological-sessions`

---

## 🌍 قائمة الدول المدعومة

جميع النماذج تدعم 20 دولة مع التحقق من صحة أرقام الهواتف:

| الدولة | الكود | عدد الأرقام |
|--------|------|-------------|
| 🇪🇬 مصر | +20 | 10 |
| 🇸🇦 السعودية | +966 | 9 |
| 🇦🇪 الإمارات | +971 | 9 |
| 🇰🇼 الكويت | +965 | 8 |
| 🇴🇲 عمان | +968 | 8 |
| 🇶🇦 قطر | +974 | 8 |
| 🇧🇭 البحرين | +973 | 8 |
| 🇯🇴 الأردن | +962 | 9 |
| 🇱🇧 لبنان | +961 | 8 |
| 🇸🇾 سوريا | +963 | 9 |
| 🇮🇶 العراق | +964 | 10 |
| 🇾🇪 اليمن | +967 | 9 |
| 🇱🇾 ليبيا | +218 | 9 |
| 🇩🇿 الجزائر | +213 | 9 |
| 🇹🇳 تونس | +216 | 8 |
| 🇲🇦 المغرب | +212 | 9 |
| 🇸🇩 السودان | +249 | 9 |
| 🇵🇸 فلسطين | +970 | 9 |
| 🇺🇸 أمريكا/كندا | +1 | 10 |
| 🇬🇧 بريطانيا | +44 | 10 |

---

## 🎨 مميزات النماذج

### ✅ التصميم
- **متجاوبة تماماً:** تعمل على جميع الأحجام
- **ملء الشاشة:** على الهواتف تملأ الشاشة بالكامل
- **واسعة على الشاشات الكبيرة:** max-width تصل إلى 4xl على الشاشات الكبيرة
- **بدون تمرير في الخلفية:** عند فتح النافذة لا يمكن تمرير الموقع

### ✅ التحقق من البيانات
- **التحقق من أرقام الهواتف:** بناءً على الدولة المختارة
- **حقول إلزامية:** جميع الحقول المهمة مطلوبة
- **تنسيق تلقائي:** أرقام الهواتف تزيل أي حرف غير رقمي

### ✅ تجربة المستخدم
- **نص أسود:** جميع المدخلات بنص أسود واضح
- **رسائل خطأ واضحة:** بالعربية
- **رسالة نجاح:** مع أيقونة تأكيد
- **زر واتساب:** للتواصل الفوري

### ✅ الأمان
- **سرية المعلومات:** ملاحظة في النماذج النفسية
- **حفظ في Firebase:** جميع البيانات محمية

---

## 📱 كيفية الاستخدام

### في أي مكون:

```typescript
import IjazahForm from './components/IjazahForm';
import SessionForm from './components/SessionForm';
import { useState } from 'react';

function YourComponent() {
  const [isIjazahOpen, setIsIjazahOpen] = useState(false);
  const [isSessionOpen, setIsSessionOpen] = useState(false);
  const [isConsultationOpen, setIsConsultationOpen] = useState(false);

  return (
    <>
      {/* زر التقدم للإجازة */}
      <button onClick={() => setIsIjazahOpen(true)}>
        التقدم للحصول على الإجازة
      </button>

      {/* زر الجلسات النفسية */}
      <button onClick={() => setIsSessionOpen(true)}>
        حجز جلسة نفسية
      </button>

      {/* زر الاستشارات النفسية */}
      <button onClick={() => setIsConsultationOpen(true)}>
        استشارة نفسية
      </button>

      {/* النماذج */}
      <IjazahForm 
        isOpen={isIjazahOpen} 
        onClose={() => setIsIjazahOpen(false)} 
      />

      <SessionForm 
        sessionType="جلسات نفسية"
        isOpen={isSessionOpen} 
        onClose={() => setIsSessionOpen(false)} 
      />

      <SessionForm 
        sessionType="استشارات نفسية"
        isOpen={isConsultationOpen} 
        onClose={() => setIsConsultationOpen(false)} 
      />
    </>
  );
}
```

---

## 🗄️ قواعد البيانات Firebase

### مجموعة: `ijazah-applications`
```javascript
{
  name: string,
  phone: string,          // مع كود الدولة (مثال: +201234567890)
  whatsapp: string,       // مع كود الدولة
  city: string,
  age: string,
  education: string,
  quranLevel: string,
  readingAbility: string,
  notes: string,
  createdAt: timestamp,
  status: 'pending' | 'approved' | 'rejected'
}
```

### مجموعة: `psychological-sessions`
```javascript
{
  sessionType: 'جلسات نفسية' | 'استشارات نفسية',
  name: string,
  phone: string,          // مع كود الدولة
  whatsapp: string,       // مع كود الدولة
  city: string,
  age: string,
  sessionPreference: 'أونلاين' | 'حضوري',
  preferredTime: string,
  issueDescription: string,
  createdAt: timestamp,
  status: 'pending' | 'confirmed' | 'completed' | 'cancelled'
}
```

---

## 🔒 قواعد Firestore المطلوبة

أضف هذه القواعد في Firebase Console:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // الحجوزات الحالية
    match /bookings/{booking} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
    
    // طلبات الإجازة
    match /ijazah-applications/{application} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
    
    // الجلسات والاستشارات النفسية
    match /psychological-sessions/{session} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
  }
}
```

---

## 🎯 التحديثات على النماذج الحالية

### BookingForm.tsx (تم التحديث)
- ✅ منع التمرير في الخلفية عند الفتح
- ✅ واسعة على الشاشات الكبيرة (max-w-2xl, lg:max-w-3xl, xl:max-w-4xl)
- ✅ تحسين التجاوب

---

## 📊 لوحة التحكم

للوصول إلى البيانات الجديدة، يمكنك:

1. **Firebase Console مباشرة:**
   - https://console.firebase.google.com/project/mostafa-acdemy/firestore/data

2. **إنشاء صفحات إدارة منفصلة** (اختياري في المستقبل):
   - `/admin-ijazah.html` - لإدارة طلبات الإجازة
   - `/admin-sessions.html` - لإدارة الجلسات النفسية

---

## 🚀 الخطوات التالية

1. ✅ أضف أزرار الحجز في الأماكن المناسبة بالموقع
2. ✅ اختبر جميع النماذج
3. ✅ تحديث قواعد Firestore
4. ✅ (اختياري) إنشاء لوحات تحكم منفصلة للنماذج الجديدة

---

## 📝 ملاحظات مهمة

1. **السرية:** النماذج النفسية تحتوي على تنويه عن سرية المعلومات
2. **التحقق:** جميع النماذج تتحقق من صحة أرقام الهواتف
3. **الواتساب:** الرقم المستخدم: 201066660096 (يمكن تغييره من كل ملف)
4. **منع التمرير:** تلقائياً عند فتح أي نموذج

---

تم إنشاء النظام بنجاح! 🎉
